import { ToggleControlDirective } from './toggle-control.directive';

describe('ToggleControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleControlDirective();
    expect(directive).toBeTruthy();
  });
});
